{
  "name": "lucidledger-contracts",
  "version": "1.0.0",
  "description": "Smart contracts for Lucid Ledger - Escrow-based work contracts",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:factory": "hardhat run scripts/deployFactory.js --network baseSepolia",
    "verify": "hardhat verify --network baseSepolia"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.19.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0"
  }
}
