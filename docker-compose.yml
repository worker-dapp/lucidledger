services:
  frontend:
    build: ./client
    container_name: client
    ports:
      - "5173:5173"
    env_file:
      - ./client/.env
    restart: unless-stopped
    networks:
      - app-network

networks:
  app-network:
    driver: bridge